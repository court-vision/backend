[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: fast isolated tests (no DB, no network)
    api: API endpoint tests with TestClient
    integration: database-backed tests requiring PostgreSQL
